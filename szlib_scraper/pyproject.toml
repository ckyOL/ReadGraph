[project]
name = "szlib-scraper"
version = "0.1.0"
description = "深圳图书馆借阅历史抓取工具"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
