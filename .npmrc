# ===================================================
# ReadGraph 注册表/认证配置
# 本文件受版本控制，未经安全审查不得修改
# 参考规范: docs/npm-supply-chain-security.md
# ---------------------------------------------------
# 注意：自 pnpm 11 起，pnpm 仅从 .npmrc 读取「注册表与认证」类设置；
#       ignore-scripts / save-exact / min-release-age / audit 等安全策略
#       已不再从此文件读取，统一迁移到 pnpm-workspace.yaml（见
#       minimumReleaseAge 等）与 pnpm 11 的默认供应链保护。
#       npm CLI 仍可读取本文件作为回退。
# ===================================================

# [官方注册表] 仅使用官方 npm 注册表
registry=https://registry.npmjs.org/

# [强制 HTTPS] registry 通信强制 TLS（pnpm/npm 均生效；pnpm 默认即为 true）
strict-ssl=true

# 认证令牌一律通过环境变量注入，禁止在本文件明文写入 _authToken。